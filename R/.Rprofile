# Set {devtools} and {usethis} to load with each R session
if (interactive()) {
  suppressMessages(require(devtools))
  suppressMessages(require(usethis))
}

options(
  # Useful {usethis} defaults for package development
  usethis.description = list(
    `Authors@R` = 'person("Patrick", "Hogan", email = "pmhogan@users.noreply.github.com", role = c("aut", "cre"))',
    License = "GPL-3",
    Version = "0.0.0.9000"
  ),
  usethis.full_name = "Patrick Hogan",
  usethis.protocol = "https",
  # If using VS code
  # vsc.rstudioapi = TRUE,
  # Helpful warnings when developing packages
  warnPartialMatchArgs = TRUE,
  warnPartialMatchDollar = TRUE,
  warnPartialMatchAttr = TRUE
)
